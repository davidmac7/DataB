{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.8.2",
    "bootstrap": "^5.3.3",
    "cra-template": "1.2.0",
    "cross-env": "^7.0.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.5",
    "react-scripts": "5.0.1",
    "react-signature-canvas": "^1.1.0-alpha.1",
    "web-vitals": "^4.2.4"
  },
  "scripts": {
    "start": "cross-env HTTPS=true SSL_CRT_FILE=aviation-tool.local.pem SSL_KEY_FILE=aviation-tool.local-key.pem HOST=aviation-tool.local PORT=3000 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
